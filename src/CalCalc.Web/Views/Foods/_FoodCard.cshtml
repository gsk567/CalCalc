@model FoodModel

<div class="card">
    <div class="card-header">
        <h3 class="card-title">
            @Model.Name
        </h3>
    </div>
    <div class="card-body">
        <p>Calories: @Model.Calories</p>
    </div>
</div>